{
  "build": {
    "builder": "Docker"
  },
  "deploy": {
    "startCommand": "gunicorn rag_django.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}